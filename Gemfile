source "https://rubygems.org"

# Use the GitHub Pages gem to match the live environment
gem "github-pages", group: :jekyll

# Optional: Add any extra plugins if you use them in the future
# gem "jekyll-feed", "~> 0.12"
